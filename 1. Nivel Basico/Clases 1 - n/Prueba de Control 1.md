# Prueba de Control #1: Variables y Tipos de Datos

Esta prueba evaluará tu comprensión teórica y práctica de las dos clases que hemos tenido. Para aprobar y avanzar a la Clase 3, debes responder correctamente con un 95% de precisión.

Instrucciones: Lee detenidamente cada punto y responde escribiendo el código o la explicación solicitada.

## Pregunta 1: Teoría (30%)
Explica con tus propias palabras: ¿Cuál es la diferencia fundamental entre declarar una variable con let y declararla con const?

## Pregunta 2: Depuración de Código (30%)
El siguiente código tiene un error crítico que hará que JavaScript falle al ejecutarse. Identifica el error, explícame por qué ocurre y reescribe el código de forma correcta.

    const puntosDeVida = 100;
    console.log("Puntos iniciales:", puntosDeVida);

    puntosDeVida = 80;
    console.log("Puntos tras el ataque:", puntosDeVida);

## Pregunta 3: Creación desde cero (40%)
Crea un pequeño script que cumpla con las siguientes reglas:

    Declara una variable inmutable (que no cambia) para el nombre de tu mascota o animal favorito (ej. "Perro").
    Declara una variable mutable para su edad (ej. 5).
    Declara una variable que indique si está dormido o no (usando el tipo de dato correcto para un estado de sí/no).
    Imprime en un solo console.log() los tres valores.
    Imprime en otro console.log() el tipo de dato de la variable que indica si está dormido.
